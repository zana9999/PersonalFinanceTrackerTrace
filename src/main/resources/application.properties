spring.application.name=personal-expense-tracker
spring.datasource.url=jdbc:mysql://localhost:3306/expense_tracker?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=databasepassword
# change pass if launching (personal use)
#pass removed due to privacy 
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Date handling
spring.jackson.date-format=yyyy-MM-dd
spring.jackson.time-zone=UTC

# Server configuration
server.port=8080

# Logging for debugging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.personal_expense_tracker_com.example.personal_expense_tracker=DEBUG
logging.level.org.springframework.web=DEBUG

# application.properties
#frontend.origin=https://your-frontend-domain.com
